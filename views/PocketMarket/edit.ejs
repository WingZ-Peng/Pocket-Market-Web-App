<% include ../partials/header %>
<div class="container">
    <div class="row">
        <div col-lg-12 col-md-12 col-sm-12>
            <h1 style="text-align:center;">Edit <%=pocketMarket.name %></h1>
            <div style="width:30%; margin:25px auto;">
                <form action="/PocketMaret/<%=pocketMarket._id %>?_method=PUT" method="POST" enctype="multipart/form-data">
                    <div class="form-group">
                        <label for="pocketMarket[name]">Name</label>
                        <input class="form-control" type="text" name="pocketMarket[name]" value="<%= pocketMarket.name %>">
                    </div>
                    <div class="form-group"> 
                        <label for="image">New Image (Leave empty if not updating)</label>
                        <input type="file" id="image" name="image" accept="image/*" >
                    </div>
                    <div class="form-group">
                        <label for="pocketMarket[description]">Description</label>
                        <input class="form-control" type="text" name="pocketMarket[description]" value="<%= pocketMarket.description%>">
                    </div>
                    <div class="form-group"> 
                        <label for="pocketMarket[price">Price</label>
                        <input class="form-control" type="number" name="pocketMarket[price]" value="<%= pocketMarket.price%>">
                    </div>
                    <div class="form-group"> 
                   <button class="btn btn-lg btn-primary btn-block">Submit</button>
                    </div>
                </form>
                <a href="/PocketMarket/<%=pocketMarket._id %>" class="btn btn-xs btn-warning" >Go Back</a>
            </div>
        </div>
    </div>
</div>

<% include ../partials/footer %>